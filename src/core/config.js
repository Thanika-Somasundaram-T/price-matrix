const shops = ['Shop 1', 'Shop 2', 'Shop 3'];
const products = ['Carrot', 'Guava', 'Apple', 'Banana'];

const config = {
	shopInput: shops[0],
	productInput: products[0],
	priceInput: '',
	IdLength: 12,
	shops: shops,
	products: products,
};

export default config;
